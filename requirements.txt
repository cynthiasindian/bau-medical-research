# --- Core (always required) ---
streamlit>=1.34
pandas>=2.0
openpyxl>=3.1        # read/write .xlsx
filelock>=3.12       # safe concurrent writes to the Excel file

# --- Only needed if you switch to Google Sheets ---
gspread>=6.0
google-auth>=2.0

# --- TOML config reader fallback for Python < 3.11 ---
tomli>=2.0 ; python_version < "3.11"
